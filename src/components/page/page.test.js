import React from 'react';
import Page from './page';
import { shallow } from 'enzyme';
import { expect } from 'chai';

describe('Page', () => {
    it('should render a card for the highest and lowest generated energy sources in date range', () => {

    })
})